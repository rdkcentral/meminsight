# OpenSpec Changes

This directory is for active delta specs that represent intentional behavior changes from the current baseline.

## How to use

1. Add a delta spec before implementation when behavior changes are intended.
2. Reference impacted capability IDs from openspec/specs.
3. Remove active delta specs after merge and baseline promotion is complete.

## Baseline state

When no active behavior changes are in progress, this directory should contain only baseline markers.
